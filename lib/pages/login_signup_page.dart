import 'package:flutter/material.dart';

class LoginSignUpPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Login/SignUp'),
      ),
      body: Center(
        child: Text('Login/SignUp Placeholder'),
      ),
    );
  }
}
